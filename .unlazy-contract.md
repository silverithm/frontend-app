# 어르신 케어 정보(careProfile) API 계약 — 백엔드·웹·앱 공용

기존 어르신 API(`ElderController`)를 확장한다. `ElderlyDTO` record(5필드)는 배차 코드가 널리 쓰므로
**그대로 두고**, 회사 단위 목록 응답에만 `careProfile`을 덧붙인 새 응답 DTO를 쓴다.

## careProfile 객체 (JSON)

| 키 | 타입 | 방향 | 설명 |
|---|---|---|---|
| residentNumberMasked | string\|null | 응답만 | `"410203-2******"` 형태. 앞 6자리 + 하이픈 + 뒤 첫 자리 + `******`. 미등록이면 null |
| residentNumber | string\|null | 요청만 | 13자리 숫자(하이픈 허용, 저장 시 제거). **null/미포함 = 기존 값 유지, `""` = 삭제**. AES-GCM 암호화 저장 |
| birthDate | `YYYY-MM-DD`\|null | 양방향 | 요청에 없고 residentNumber가 있으면 주민번호로 파생(뒤 첫 자리 1·2=19xx, 3·4=20xx, 9·0=18xx) |
| gender | `MALE`\|`FEMALE`\|null | 양방향 | 요청에 없으면 주민번호로 파생(1·3·9=MALE, 2·4·0=FEMALE) |
| age | int\|null | 응답만 | birthDate 기준 만 나이(오늘 기준) |
| careGrade | `GRADE_1`~`GRADE_5`\|`COGNITIVE_SUPPORT`\|`NONE`\|null | 양방향 | 장기요양등급 |
| fallRisk | bool | 양방향 | 낙상 위험 |
| fallNote | string\|null | 양방향 | 암호화 저장 |
| pressureSore | bool | 양방향 | 욕창 (있음/위험) |
| pressureSoreNote | string\|null | 양방향 | 암호화 |
| diaperType | `NONE`\|`PANTY`\|`PAD`\|`BOTH`\|null | 양방향 | 기저귀(팬티기저귀/패드) |
| diaperIntermittent | bool | 양방향 | 간헐적 사용 |
| cognitionLevel | `NORMAL`\|`MILD`\|`MODERATE`\|`SEVERE`\|null | 양방향 | 인지 |
| cognitionNote | string\|null | 양방향 | 암호화 |
| mealType | `REGULAR`\|`CHOPPED`\|`PORRIDGE`\|`MIXED`\|null | 양방향 | 일반식/다진식/죽/비빔식 |
| morningSnack | bool | 양방향 | 오전간식 (기본 true) |
| afternoonSnack | bool | 양방향 | 오후간식 (기본 true) |
| dinner | bool | 양방향 | 저녁식사 (기본 true) |
| mealNote | string\|null | 양방향 | 기피식품·대체식품·기간 조건. 암호화 |
| bathTime | string\|null | 양방향 | 예 `"9:40-50"` (≤50자) |
| bathNote | string\|null | 양방향 | 예 "방문목욕으로 1,3째주 센터 내 목욕 안 함" |
| medMorning / medLunch / medEvening | bool | 양방향 | 투약 아침/점심/저녁 |
| medMorningTime / medLunchTime / medEveningTime | string\|null | 양방향 | 예 `"10시"` (≤20자) |
| medNote | string\|null | 양방향 | 암호화 |
| vehicleNote | string\|null | 양방향 | 차량(이용 차량·등하원 특이사항, ≤255자) |
| floor | int\|null | 양방향 | 자리 층(1/2…) |
| seatNote | string\|null | 양방향 | 자리 위치 메모(예 "TV 앞 좌측", ≤100자) |
| careNote | string\|null | 양방향 | 기타 메모. 암호화 |
| updatedAt | ISO datetime\|null | 응답만 | |

문자열 note 계열은 최대 평문 500자. 암호화 컬럼은 VARCHAR(2048)로 잡는다(암호문 ≈ 4.2배).

## 엔드포인트

1. `GET /api/v1/elders/company/{companyId}` → `{ "elders": [ { id, name, homeAddress, requiredFrontSeat, homeAddressName, careProfile } ] }`
   - careProfile은 프로필이 없으면 **null**. 전체 주민번호는 절대 포함하지 않는다(마스킹만).
   - 호출자 기관 검증: `resourceScopeGuard.requireSameCompany(companyId)` 추가.
2. `POST /api/v1/elders/company/{companyId}` body `{ name, homeAddress?, requiredFrontSeat?, careProfile? }`
3. `POST /api/v1/elders/company/{companyId}/bulk` body `[ { name, homeAddress?, requiredFrontSeat?, careProfile? } ]` → `{ created }`
4. `PUT /api/v1/elders/company/elder/{id}` body와 동일. **careProfile 미포함(null) = 프로필 건드리지 않음**, 포함 = 그 객체로 전체 갱신(단 residentNumber null은 유지).
5. `PUT /api/v1/elders/company/elder/{id}/care-profile` body = careProfile → 응답 careProfile (앱·웹 공용, 이름은 안 바꿈)
6. `GET /api/v1/elders/company/elder/{id}/resident-number` → `{ "residentNumber": "4102032830514" }`
   - **관리자만**: principal이 AppUser(이메일)이거나 Member.role == ADMIN. 아니면 403.
   - 열람을 `log.info("[PII] 주민번호 열람 elderId={} by={}")`로 남긴다.
7. `DELETE /api/v1/elders/company/elder/{id}` 신설(기관 검증 포함) — 프록시 DELETE가 지금 `/api/v1/elder/{id}`(검증 없음)를 부른다. 프록시도 새 경로로 바꾼다. 프로필은 FK ON DELETE CASCADE.

## DB (Flyway `V1.92.0__Create_Elder_Care_Profile.sql`)

테이블 `elder_care_profile`: `elderly_id BIGINT PK, FK → elderly(node_id) ON DELETE CASCADE`, 위 컬럼들(snake_case),
`created_at DATETIME(6)`, `modified_at DATETIME(6)`. bool 컬럼은 `NOT NULL DEFAULT FALSE`(간식·저녁만 DEFAULT TRUE).
enum은 VARCHAR(20). JPA는 `@OneToOne @MapsId` 또는 `@Id` = elderly_id.

## 웹 프록시 (frontend-admin `src/app/api/v1/...`)

새 라우트: `elders/company/elder/[id]/care-profile/route.ts`(PUT), `elders/company/elder/[id]/resident-number/route.ts`(GET).
기존 `elders/company/elder/[id]/route.ts`의 DELETE는 `/api/v1/elders/company/elder/{id}`로 변경.
