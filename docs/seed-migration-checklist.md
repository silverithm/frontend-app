# Seed 이식 전수 체크리스트
갱신: 2026-08-06
## 사용법: 파일별로 전환 완료 시 [x] 표시. 발견 항목이 모두 해소되면 완료.

감사 범위: `lib/screens/*.dart` 전체(33개) + `lib/widgets/`(common/, approval/, seed/ 제외, 실질 파일 10개 — `widgets/chat/index.dart`, `widgets/notice/index.dart`는 3줄 export 배럴이라 감사 대상에서 제외).
감사 방법: grep으로 위치 특정 후 정독(context 확인)으로 판정. docs/seed-foundations.md(레이아웃/스페이싱/라운드/그림자 원칙) + docs/seed-component-specs.md(컴포넌트 스펙) 기준.

**중요 정정**: 초기 광범위 grep은 `Colors\.` 패턴이 `AppSemanticColors.*`/`AppColors.*`(정상 토큰 클래스)까지 오탐지했다. 정밀 재검증(`[^a-zA-Z.]Colors\.`) 결과 순수 Flutter `Colors.*` 하드코딩은 코드베이스 전체에서 사실상 없음(예외 3곳만, 아래 표에 명시). 색상 토큰화는 이미 전면적으로 잘 되어 있고, 남은 잔재는 주로 **그라디언트 / 정적 컨테이너 그림자 / 구 버튼(shadcn·AppButton) / 칩 패턴 / 임의 radius·padding 리터럴**에 집중되어 있다.

| # | 파일 | 상태 | 남은 잔재 (종류: 위치 대략) |
|---|---|---|---|
| 1 | screens/admin_approval_management_screen.dart | [x] | 없음 |
| 2 | screens/admin_approval_template_screen.dart | [x] | 없음 |
| 3 | screens/admin_company_settings_screen.dart | [x] | 그라디언트 1곳+정적그림자 5곳(회사정보카드/상세정보/관리자정보/구독정보/로그아웃 섹션) 모두 제거→단색 배경+1px 보더. shadcn버튼 13곳 전부 SeedButton으로(회원탈퇴=critical, 로그아웃=neutralOutline). shadcn.AlertDialog 4곳(로그아웃/구독일시정지/구독재개/회원탈퇴)을 AppDialog.showConfirm·showCustom+SeedButton 조합으로 교체. radius·padding 리터럴 전면 AppSpacing/AppBorderRadius 토큰화. analyze 0(기존 print/toList info만 잔존, 로직 변경 없음). |
| 4 | screens/admin_notice_form_screen.dart | [x] | 그라디언트/그림자/shadcn/AppButton/칩/radius·padding 리터럴 전부 0. 완료 상태 |
| 5 | screens/admin_notice_management_screen.dart | [x] | shadcn버튼 6곳(작성 CTA/재시도/필터 초기화·적용/삭제 확인 취소·삭제) 전부 SeedButton으로(재시도=neutralWeak, 필터 초기화=neutralWeak small, 그 외 brandSolid). shadcn.AlertDialog(삭제 확인)를 `AppDialog.showConfirm`으로 교체. `_buildFilterChip`의 `FilterChip`→`SeedChip`. 스와이프 삭제 배경의 `AppColors.white`→`AppSemanticColors.textInverse` 시맨틱 토큰화. shadcn 의존 완전 제거. analyze 0(이슈 없음, 로직 변경 없음). |
| 6 | screens/admin_payment_screen.dart | [x] | `_buildPlanSummary` 그라디언트+정적그림자, `_buildCompanyInfo`/`_buildPaymentInfo`/`_buildTermsAgreement` 정적그림자 모두 제거→단색/1px 보더. shadcn버튼(결제하기/성공다이얼로그 완료) SeedButton으로, shadcn.AlertDialog 2곳(성공/실패)을 AppDialog.showCustom·showAlert로 교체. radius·padding 리터럴 토큰화. 결제/웹뷰 로직 변경 없음. analyze 0(기존 print/null-comparison 잔존, 로직 변경 없음). |
| 7 | screens/admin_unified_approval_screen.dart | [x] | 단순 TabBar 셸(하위 화면에 위임). 전 카테고리 0. 완료 상태 |
| 8 | screens/admin_user_management_screen.dart | [x] | 없음 |
| 9 | screens/admin_vacation_limits_setting_screen.dart | [x] | **정적그림자 3곳 제거**: "월 선택 헤더"/"저장 버튼 바" Container의 BoxShadow→상하 1px 보더로, "일별 카드"(`_buildDateCard`)는 이미 보더가 있어 BoxShadow만 제거. **카드중첩 평탄화**: `_buildLimitInputCard`(인원수 입력 박스)를 감싸던 회색 배경+보더 Container 제거→날짜 카드 헤더 아래 Divider 1px로 구분되는 평면 Column으로. shadcn.PrimaryButton(저장하기)→SeedButton(brandSolid, large, isLoading 연동). radius·padding 리터럴 전면 AppSpacing/AppBorderRadius 토큰화. shadcn 의존 완전 제거. analyze 0(기존 unused_import(app_theme)/invalid_null_aware_operator/print 잔존, 로직 변경 없음). |
| 10 | screens/admin_vacation_management_screen.dart | [x] | **죽은 코드 삭제**: `_buildVacationList`/`_buildVacationCard`/`_buildApprovalTab`/`_buildLimitsTab`/`_buildHistoryTab`(L473-1401 부근, 호출부 전무 확인) 통째로 삭제 — 파생된 미사용 `_showLimitSettingDialog`·`admin_vacation_limits_setting_screen.dart` import도 함께 정리. 이로써 카드중첩 의심이던 Card( 계열 5곳도 함께 해소. 남은 shadcn버튼(일괄승인·거절/행별 삭제·거절·승인) 전부 SeedButton으로(승인=brandSolid, 반려=critical, 삭제=critical). shadcn.AlertDialog 3곳(개별 삭제/일괄 승인/일괄 거절)을 `AppDialog.showConfirm`으로 교체. `_buildStatusFilterChip`/`_buildRoleFilterChip`/`_buildSortFilterChip`는 이미 SeedChip 사용 중(칩 패턴 잔존 아님, 확인 완료). radius·padding 리터럴 전면 AppSpacing/AppBorderRadius 토큰화. shadcn 의존 완전 제거(66KB→약 40KB로 축소). analyze 0(기존 unused_import(constants/app_theme)·unused_field(_vacationLimits)·invalid_null_aware_operator·print 등 잔존, 로직 변경 없음). |
| 11 | screens/approval_detail_screen.dart | [x] | 삭제 확인 다이얼로그(shadcn.AlertDialog+OutlineButton/DestructiveButton)를 `AppDialog.showConfirm`(파괴적)으로 교체. "문서 미리보기" OutlinedButton→SeedButton neutralOutline. 다이얼로그·스낵바 radius 리터럴(12) 전부 AppBorderRadius.xl 토큰화. shadcn 의존 완전 제거. analyze 0(기존 invalid_null_aware_operator/print 잔존, 로직 변경 없음). |
| 12 | screens/approval_form_screen.dart | [x] | shadcn.PrimaryButton 제출 CTA→SeedButton brandSolid large(isLoading 연동). OutlinedButton("양식 문서 바로 작성하기")→SeedButton neutralOutline. DropdownButtonFormField의 itemHeight 리터럴(56)→AppSpacing.space14. shadcn 의존 완전 제거. analyze 0(기존 invalid_null_aware_operator/print/deprecated 잔존, 로직 변경 없음). |
| 13 | screens/approval_list_screen.dart | [x] | 로딩/에러/빈 상태 박스의 radius·padding 리터럴(20/32/24/16 등) 전부 AppSpacing/AppBorderRadius 토큰화. shadcn.PrimaryButton 재시도→SeedButton neutralWeak. 빈 상태 문구를 "아직 결재 내역이 없어요" / "우측 하단 버튼을 눌러 새 결재를 작성해보세요"로 재적용. shadcn 의존 완전 제거. analyze 0(기존 unused_import(app_theme/approval_status_badge) 잔존, 로직 변경 없음). |
| 14 | screens/calendar_screen.dart | [x] | 일정 삭제 확인 native AlertDialog+TextButton 2곳→AppDialog.showConfirm. 등록 바텀시트 raw ElevatedButton→SeedButton(brandSolid, large). 바텀시트 제목/내용/장소 TextField 3곳→SeedTextField, DropdownButtonFormField는 라디우스 토큰 정리(변경 없이 유지). "하단 통계 섹션"(L1104 부근) 정적그림자 제거→보더만. "선택된 날짜" 카드 안 빈상태 박스(휴무/일정 탭 2곳) 카드중첩 평탄화→Padding+Row로. 핸들·라디우스 잔여 리터럴 AppSpacing/AppBorderRadius 토큰화. 날짜 그리드 셀 로직 미변경. analyze 0(기존 print/미사용 import/deprecated value 등만 잔존, 로직 변경 없음). |
| 15 | screens/chat_room_info_screen.dart | [x] | shadcn버튼 6곳(하단 "채팅방 나가기"=critical, "참가자 초대"=neutralWeak, 나머지 4곳은 다이얼로그 내부)을 SeedButton으로, shadcn.AlertDialog 2곳(나가기/강제퇴장 확인)을 AppDialog.showConfirm으로 교체. 아이콘 원형 컨테이너 width/height 리터럴(80/40)을 AppSpacing.space20/space10으로 토큰화. shadcn 의존 완전 제거. analyze 0 error, 로직 변경 없음. |
| 16 | screens/chat_room_list_screen.dart | [x] | shadcn.PrimaryButton(빈 상태 CTA) 제거, 빈 상태 문구를 "아직 채팅방이 없어요" / "+ 버튼으로 첫 채팅을 시작해보세요"로 교체(다른 화면과 동일하게 상단 + 아이콘 버튼을 가리키는 안내형으로 전환, 별도 CTA 버튼 없음). 기존에 없던 에러 상태 분기(`chatProvider.errorMessage` 비어있지 않을 때)를 추가하고 SeedButton(neutralWeak, "다시 시도")로 재조회. shadcn 의존 완전 제거. analyze 0 error, 로직 변경 없음(에러 상태 UI만 신규 추가, provider 호출은 기존 `_loadChatRooms` 재사용). |
| 17 | screens/chat_room_screen.dart | [x] | `_buildMessageInput` 하단 입력바 Container(BoxShadow blurRadius10)를 widgets/chat/message_input.dart와 동일하게 상단 `AppSemanticColors.borderSubtle` 1px 보더로 교체(중복 그림자 제거). 말풍선 radius는 이미 AppBorderRadius 토큰 사용 중임을 확인(변경 불필요), 말풍선 색(`interactivePrimaryDefault`)도 brandDefault와 동일한 teal600으로 정합 확인. 날짜 구분선 UI는 이 화면에 존재하지 않음(해당 없음). shadcn.AlertDialog 4곳(채팅방 삭제 확인/메시지 삭제 확인/리액션 사용자 목록/다운로드 진행)을 AppDialog로 전환: 확인형 2곳(채팅방·메시지 삭제)은 `AppDialog.showConfirm`(취소=neutralWeak, 삭제=critical 톤), 나머지 2곳은 `AppDialog.showCustom`으로 교체하고 리액션 목록 다이얼로그의 닫기 버튼은 SeedButton(brandSolid)으로 명시 전환. shadcn 버튼 5곳(OutlineButton 취소×2, DestructiveButton 삭제×2, GhostButton 닫기×1) 전부 해소, shadcn import 완전 제거. analyze 0 error, STOMP·provider 로직 변경 없음. |
| 18 | screens/create_chat_room_screen.dart | [x] | shadcn.GhostButton 3곳("만들기" AppBar 액션=neutralWeak small, "전체 선택"=brandWeak small, "선택 해제"=neutralWeak small)을 SeedButton으로 교체. 이름/설명 TextField 2곳을 SeedTextField로 교체(기존 별도 라벨 Text+SizedBox 제거, SeedTextField 내장 라벨로 통합). shadcn 의존 완전 제거. analyze 0 error, 로직 변경 없음. |
| 19 | screens/home_screen.dart | [x] | 카드중첩 해소 완료: `_SectionCard`를 shadcn.Card→평평한 흰 표면(borderSubtle 1px+xl2 라운드)으로 교체(shadcn 의존 제거). `_DashboardMetricCard` 4타일은 틴트+보더 카드→아이콘 칩+숫자+라벨 평면 블록. `_NoticePreviewTile`/`_SchedulePreviewTile`/`_EmptySectionState`는 개별 보더 컨테이너→구분선(Divider) 리스트 아이템. `_SectionHeader`의 "전체보기" TextButton은 유지하되 styleFrom으로 토큰화. analyze 0. |
| 20 | screens/hwp_editor_screen.dart | [x] | raw AppColors 팔레트(teal600/teal50/teal700/teal500/gray100/gray500/gray400/red500)를 AppSemanticColors로 전환, 헤더/배너/로딩/에러 TextStyle 리터럴을 AppTypography로, 패딩 리터럴을 AppSpacing으로 토큰화. 저장 버튼(TextButton.icon)과 재시도 버튼(OutlinedButton)을 SeedButton으로 교체. analyze 0. |
| 21 | screens/login_screen.dart | [x] | 비밀번호 찾기 `shadcn.AlertDialog`(L130)를 `AppDialog.showCustom`+`Padding`+`Column`(내부 `StatefulBuilder`로 사용자 타입 토글 상태 유지)으로 교체. 액션 버튼 `shadcn.OutlineButton`/`shadcn.PrimaryButton`(L193·198)→`SeedButton`(취소=neutralOutline, 비밀번호 찾기=brandSolid, `authProvider.isLoading` 연동 유지)으로 교체. shadcn_flutter 의존 완전 제거(import 삭제). analyze 0 error(기존 unused_import(shared_preferences/constants)·avoid_print·use_build_context_synchronously 잔존, 로직 변경 없음). |
| 22 | screens/main_screen.dart | [x] | 하단 탭 셸, 자체 AppBar 없음(양호, 이중헤더 없음). 탭 아이콘 배경 radius12→AppBorderRadius.xl. 뱃지 padding(horizontal:4/vertical:1)→AppSpacing.space1/px, minWidth16→AppSpacing.space4. 아이콘 size 24/22, 뱃지 fontSize10은 AppSpacing/AppTypography 스케일에 없는 값이라 화면 전용 명명 상수(`_navIconSizeSelected`/`_navIconSizeUnselected`/`_navBadgeFontSize`)로 관리. 뱃지 합산 로직(승인/휴무/가입대기/채팅 미읽음) 무변경. analyze 0. |
| 23 | screens/menu_screen.dart | [x] | Seed 컴포넌트 5곳 사용, 그 외 전 카테고리 0(초기 광범위 grep의 "AppButton" 1건은 실제 인스턴스화 없는 오탐으로 확인됨). 완료 상태 |
| 24 | screens/my_vacation_screen.dart | [x] | radius/padding 리터럴 전면 AppSpacing/AppBorderRadius 토큰화 완료. shadcn.OutlineButton/DestructiveButton/PrimaryButton 3곳 모두 SeedButton으로 교체, shadcn.AlertDialog(삭제 확인)는 AppDialog.showCustom+SeedButton 조합으로 교체, 카드 속 장식 박스(신청 현황/상태 배지 등) 라운드·간격 토큰화. 빈 상태 문구 "아직 휴무 신청 내역이 없어요" 톤 유지. analyze 0(info 3건은 기존부터 있던 async-context 패턴, 로직 변경 없음). |
| 25 | screens/notice_detail_screen.dart | [x] | 댓글 삭제 확인 shadcn.AlertDialog+OutlineButton/DestructiveButton→`AppDialog.showConfirm`. 에러 상태 shadcn.PrimaryButton("돌아가기")→SeedButton neutralWeak. shadcn 의존 완전 제거. radius 리터럴 6곳(14/12/18/12/12/12) 토큰화 — 원형 아바타·전송버튼 3곳은 AppBorderRadius.full, 스낵바 shape 3곳은 AppBorderRadius.xl(12와 값 일치). analyze 0 error(기존 use_build_context_synchronously info만 잔존, 로직 변경 없음). |
| 26 | screens/notice_list_screen.dart | [x] | 에러 재시도 shadcn.PrimaryButton→SeedButton neutralWeak(prefixIcon 새로고침). shadcn 의존 완전 제거. 빈 상태 문구를 "아직 공지사항이 없어요" / "새 공지가 등록되면 여기에 표시돼요"로 재적용. radius·padding은 기존부터 전면 토큰화되어 있음을 확인(추가 리터럴 없음). analyze 0. |
| 27 | screens/payment_screen.dart | [x] | `_buildPlanSummary` 그라디언트+정적그림자, `_buildPaymentInfo`/`_buildPaymentMethods` 정적그림자 모두 제거→단색/1px 보더. 결제 버튼을 SeedButton(large, isLoading 연동)으로, shadcn.AlertDialog 2곳(성공/실패)을 AppDialog.showCustom·showAlert로 교체. radius·padding 리터럴 토큰화. 결제/웹뷰 로직 변경 없음. analyze 0(기존 print/미사용 import·필드 warning만 잔존, 로직 변경 없음). |
| 28 | screens/plaza_post_detail_screen.dart | [x] | 삭제 확인 native AlertDialog+TextButton→AppDialog.showConfirm. raw OutlinedButton 좋아요 버튼→토큰화된 아이콘 버튼(neutralOutline 스펙 수치 그대로, 좋아요 시 하트만 상태색). Colors.red 2곳→AppSemanticColors.statusErrorIcon. 댓글 입력 TextField(보더·hint·contentPadding)·Checkbox(activeColor) 스타일 토큰화, 댓글 카드 radius/spacing 잔여 리터럴 토큰화. analyze 0(기존 async-context info만 잔존, 로직 변경 없음). |
| 29 | screens/plaza_screen.dart | [x] | 글쓰기/자료 업로드 바텀시트 raw ElevatedButton 2곳→SeedButton(brandSolid, 업로드 버튼은 isLoading 연동). TextField 4곳→SeedTextField. DropdownButtonFormField 2곳 라디우스 토큰화(AppBorderRadius). raw Checkbox 스타일 정리(activeColor 토큰). 게시판 배지 padding/radius 리터럴 토큰화. 빈 상태 문구 복원: 소식 "아직 소식이 없어요", 게시판 "아직 게시글이 없어요 / + 버튼으로 첫 글을 남겨보세요", 자료실 "아직 등록된 자료가 없어요 / + 버튼으로 첫 자료를 올려보세요". analyze 0(기존 미사용 dart:io import만 잔존, 로직 변경 없음). |
| 30 | screens/profile_screen.dart | [x] | shadcn.Card 4곳(프로필/회사코드/설정/약관 섹션)→평평한 보더 표면(surfaceDefault+xl2 라운드+borderDefault 1px)으로 교체. shadcn.AlertDialog 5곳(비번변경/역할변경/탈퇴확인/탈퇴완료/구독상세)→`AppDialog.showCustom`+SeedButton 조합으로 교체(탈퇴=critical, 그 외 brandSolid/neutralOutline/neutralWeak). shadcn·AppButton 버튼 전부 SeedButton으로. radius·padding 리터럴(4/8/10/12/16/20/24 등) 전부 AppSpacing/AppBorderRadius 토큰화(40×40 아이콘 뱃지의 radius 20은 원 의도 보존 위해 AppBorderRadius.full로 매핑). 로그아웃/탈퇴/비밀번호 변경 로직 무변경. analyze 0(기존 unused_import/unused_field/deprecated RadioListTile/use_build_context_synchronously 잔존, 로직 변경 없음). |
| 31 | screens/register_screen.dart | [~] | 그라디언트 3곳(L593 가입프로세스 카드, L673 사용방법 버튼, L1961 프로세스카드 단계뱃지)→단색 배경(단계뱃지는 `colors[0]` 솔리드) 전환. 정적 BoxShadow 4곳(L603 가입프로세스 카드, L681 사용방법 버튼, L1893 활성단계 인디케이터, L1945 프로세스카드)은 모두 제거하고 기존/신규 1px 보더로 대체(프로세스카드는 원래 보더가 있어 그림자만 삭제). `shadcn.GhostButton`(L690, 사용방법 보러가기)→`SeedButton`(brandSolid, large, prefixIcon). shadcn_flutter import 완전 제거. **미해결(범위 밖)**: 구 `AppButton` 2곳(가입완료 안내 다이얼로그 "로그인 하러 가기"/"로그인 화면으로 이동", L180·L323 부근)은 이번 작업 지시 범위(그라디언트/그림자/GhostButton)에 포함되지 않아 미변경 — 초대형 파일(112KB)로 다른 작업자와 동시 편집 중이었어서 지정된 4개 지점만 정리. analyze 0 error(기존 unused_element(`_buildProcessStep`, 호출부 없음, 사전 존재)·avoid_print·deprecated value 잔존, 로직 변경 없음). |
| 32 | screens/signature_manage_screen.dart | [x] | raw ElevatedButton("서명 등록")→SeedButton(brandSolid, large, isLoading·isDisabled 연동, SizedBox(width: double.infinity) 풀와이드 래핑은 프로젝트 관례 유지). raw Colors.white→AppColors.white. analyze 0. |
| 33 | screens/subscription_check_screen.dart | [x] | 그라디언트 5곳(로딩화면/AppBar/환영섹션×2/플랜카드)+정적그림자 3곳(환영섹션/플랜카드 Card elevation/기능비교)+Card elevation 모두 제거→단색 배경+1px 보더. raw `Chip(`(요금제 기능 태그)를 SeedChip으로 교체. 결제 버튼을 SeedButton으로, shadcn.AlertDialog 3곳(로딩/성공/실패)을 AppDialog.showCustom·showAlert로 교체. radius·padding 리터럴 토큰화. analyze 0(기존 async-context info만 잔존, 로직 변경 없음). |
| 34 | widgets/admin_vacation_add_dialog.dart | [x] | shadcn.GhostButton/PrimaryButton(footer 액션)→SeedButton(neutralWeak/brandSolid, 등록 버튼 isLoading 연동). 수제 유형 토글(`_buildTypeOptionButton`, 필수=error 톤 오용)을 SeedChip 2개 행으로 교체(둘 다 선택 시 brandWeak 톤, 커스텀 색상 파라미터 제거, 사용처 없어진 헬퍼 메서드 삭제). Dropdown·TextFormField는 기존에 이미 AppBorderRadius/AppSpacing 토큰 적용되어 있음을 확인. maxWidth 500→480. analyze 0 error(기존 invalid_null_aware_operator/print/deprecated value 잔존, 로직 변경 없음). |
| 35 | widgets/today_schedule_dialog.dart | [x] | shadcn.OutlineButton/PrimaryButton 2곳→SeedButton(neutralWeak/brandSolid). shadcn_flutter 의존 제거. analyze 0 error, 로직 변경 없음. |
| 36 | widgets/update_dialog.dart | [x] | (갱신) L49 `shadcn.AlertDialog` 셸을 순수 Material `Dialog`(AppDialog 셸과 동일한 `AppBorderRadius.xl` 라운드 + `AppSemanticColors.surfaceDefault` 배경)+`Padding(space6)`+`Column`으로 교체 — 이 위젯은 `main.dart`에서 이미 `showDialog(builder: (_) => UpdateDialog(...))`로 호출되므로 `AppDialog.showCustom`(내부에서 showDialog 재호출) 대신 셸을 직접 구성. 제목 Text는 AppTypography.heading5, 본문/버전 텍스트는 bodyMedium 유지. 버튼은 기존처럼 SeedButton(나중에=neutralWeak, 업데이트=brandSolid)이며 Row+Expanded로 배치. `shadcn_flutter` import 완전 제거(파일 내 shadcn 의존 0). analyze 0 error(기존 use_super_parameters info만 잔존, 로직 변경 없음). |
| 37 | widgets/vacation_calendar_widget.dart | [x] | `_buildRoleFilterButton`(선택 인원 필터 pill, 구 L1346-1362)의 정적 BoxShadow(선택/비선택 2분기 모두) 제거 — 보더만으로 상태 구분. 남은 그림자 6곳은 캘린더 셀/상태점(dot)의 구조적 장식(선택 상태, 휴무유형 표시)으로 프로젝트 관례상 허용 범위이므로 유지. 그라디언트/shadcn버튼/칩 없음. analyze 0 error(기존 unused_local_variable/avoid_print/sized_box_for_whitespace 잔존, 로직 변경 없음). |
| 38 | widgets/vacation_request_dialog.dart | [x] | `_shadowSm`/`_shadowXl` 정적 BoxShadow 상수 삭제, 옵션 버튼(`_buildOptionButton`)·"선택된 날짜" 카드의 그림자 사용처는 보더로 대체. 다이얼로그 자체 표면(최외곽 Container)의 부양 그림자 1곳만 인라인으로 유지(떠 있는 오버레이 요소로 허용 범위). L377("연차/유형" 옵션 버튼 선택 그라디언트)·L519("선택된 날짜" 카드 그라디언트) 2곳 모두 단색 배경(옵션 버튼은 `selectedColor.withValues(alpha:0.15)`, 날짜 카드는 `statusInfoBackground`)으로 교체. 구 AppButton 재검토 결과 실제 인스턴스화 없음 — `AppButtonVariant.primary`는 공용 `AppDialog.showConfirm`의 `confirmVariant` 파라미터 타입으로, profile_screen.dart 등 이미 완료 처리된 파일들과 동일한 정상 패턴이라 변경 대상 아님(app_dialog.dart는 이번 작업 범위 밖). Seed 컴포넌트(SeedButton/SeedTextField) 기존 2곳 유지. analyze 0 error(기존 unused_import(app_typography/app_theme)·avoid_print·use_build_context_synchronously 잔존, 로직 변경 없음). |
| 39 | widgets/chat/chat_room_tile.dart | [x] | 전 카테고리 0, radius/색상 모두 AppBorderRadius·AppSemanticColors 토큰 사용. 완료 상태 |
| 40 | widgets/chat/message_bubble.dart | [x] | 전 카테고리 0, 말풍선 radius도 AppBorderRadius 토큰 사용(꼬리 반대쪽 xl, 꼬리쪽 base). 완료 상태 |
| 41 | widgets/chat/message_input.dart | [x] | 하단 고정 입력바 Container(L26-38)의 정적 BoxShadow 제거→상단 `AppSemanticColors.borderSubtle` 1px 보더로 교체. chat_room_screen.dart의 동일 그림자 패턴(#17)은 이번 감사 범위 밖이라 미변경. analyze 0 error(기존 unused_import(app_theme) 잔존, 로직 변경 없음). |
| 42 | widgets/notice/notice_card.dart | [x] | `NoticeCardCompact` 인디케이터 바의 radius 리터럴(L217, `BorderRadius.circular(2)`)→`AppBorderRadius.sm`, width/height 리터럴(4/40)→`AppSpacing.space1`/`AppSpacing.space10` 토큰화. 전 카테고리 0. analyze 0 error, 로직 변경 없음. |
| 43 | widgets/notice/notice_priority_badge.dart | [x] | 전 카테고리 0, AppBorderRadius 토큰 사용. 완료 상태 |

---

## 굵직한 옛 스타일 블록 상세

### 1. 홈 대시보드 지표 카드 (`home_screen.dart` `_DashboardMetricCard`, L868-934)
**평가: 이미 잘 전환됨.** `Material`+`InkWell`+`Container`(`BoxDecoration`) 구조이나:
- 그라디언트 없음, 그림자 없음(정적 카드 원칙 준수)
- `BorderRadius.circular(AppBorderRadius.xl)`, `EdgeInsets.all(AppSpacing.space4)` 등 전부 토큰 사용
- 배경/보더는 `metric.color.withValues(alpha: ...)` — 데이터 기반 시맨틱 색상, 하드코딩 아님
- 타이포는 `AppTypography.heading5`/`bodyMedium`/`bodySmall` 사용

**남은 이슈**: `_DashboardMetricCard`를 감싸는 상위 섹션 `_SectionCard`(L798-811)가 여전히 `shadcn.Card`를 사용 — 흰 표면(shadcn.Card) 안에 색상 틴트 보더 카드(`_DashboardMetricCard`, 2x2 그리드)가 들어가는 구조라 **경미한 카드 중첩 우려**가 있음(그림자는 없으므로 심각하지 않으나, `shadcn.Card` 자체를 Seed/Astryx 대응 컴포넌트로 교체 필요). 이 `_SectionCard`는 홈 화면 전체 섹션(공지, 대시보드, 커뮤니티 등)에서 반복 사용되므로 교체 시 영향 범위가 넓다.

### 2. 승인함 계열 승인/반려(거절) 버튼
- `admin_approval_management_screen.dart`: 목록 행(L947-955)과 일괄 처리 바(L710-727) 양쪽에 `shadcn.OutlineButton`(거절)+`shadcn.PrimaryButton`(승인) 페어가 반복 사용됨. 확인 다이얼로그도 `shadcn.AlertDialog`+`shadcn.OutlineButton`/`shadcn.DestructiveButton` 조합(L82-90, 325-340, 388-415).
- `approval_detail_screen.dart`: 상세 화면의 확인 다이얼로그(L92-136)도 동일하게 `shadcn.OutlineButton`/`shadcn.DestructiveButton`.
- `admin_vacation_management_screen.dart`, `admin_approval_template_screen.dart`, `admin_notice_management_screen.dart`, `chat_room_info_screen.dart`, `notice_detail_screen.dart` 등도 동일한 shadcn 확인 다이얼로그 패턴을 반복 사용 — **승인/삭제류 액션 확인 다이얼로그가 전체적으로 shadcn.AlertDialog + shadcn 버튼 조합에 묶여 있어, 이 패턴 하나를 SeedButton/Seed Dialog 조합으로 교체하면 다수 화면에 파급 효과가 있다.**

### 3. 정적 컨테이너 그라디언트+그림자 카드 (반복 패턴)
`admin_company_settings_screen.dart`(회사 정보 카드), `admin_payment_screen.dart`/`payment_screen.dart`(`_buildPlanSummary`)에서 거의 동일한 코드가 반복됨: `LinearGradient`(브랜드색 계열) + `BorderRadius.circular(20)` + `BoxShadow(blurRadius: 20, offset: Offset(0,10))`. 결제/플랜 요약 카드 스타일 하나를 표준화하면 3개 파일이 동시에 해소된다.

### 4. 하단 고정 채팅 입력바 그림자
`chat_room_screen.dart`(`_buildMessageInput`, L1443-1459)와 `widgets/chat/message_input.dart`(L26-38)가 **완전히 동일한 그림자+패딩 구조를 각자 중복 구현**하고 있다. 정적 컨테이너 그림자 규칙 위반이면서 동시에 코드 중복이므로, widgets/chat/message_input.dart 하나로 통합하며 그림자를 제거하는 것이 우선순위다.

---

## 우선순위 작업 큐 (사용자 노출 빈도 높은 순)

1. **admin_vacation_management_screen.dart** — "근무조정" 탭(관리자용, 앱 기본 선택 탭)의 핵심 화면. 감사 대상 중 최다 잔재(shadcn버튼 17, 칩패턴 14, 카드중첩 의심, 죽은 코드).
2. **my_vacation_screen.dart** — "근무조정" 탭(직원용, 기본 선택 탭). 임의 radius/padding 리터럴 최다(20/22).
3. **calendar_screen.dart** — "월간일정" 탭, 초대형 파일이지만 대부분 토큰화되어 있어 나머지 raw 버튼 3곳 + 정적 그림자 1곳만 잡으면 해소.
4. **chat_room_screen.dart** — "채팅" 탭, 메시지 화면은 진입 빈도 최상급. 하단 입력바 그림자(widgets/chat/message_input.dart와 중복) + shadcn 버튼 5곳.
5. **admin_approval_management_screen.dart** — "전자결재" 탭 승인 관리 핵심 화면. 승인/반려 버튼이 shadcn에 묶여 있어 승인 프로세스 전반에 영향.

(다음 순위: admin_company_settings_screen.dart / payment_screen.dart / admin_payment_screen.dart(그라디언트·그림자 카드 패턴, 결제 관련) → admin_user_management_screen.dart(제목 중복 포함) / admin_notice_management_screen.dart / chat_room_info_screen.dart / notice_detail_screen.dart / subscription_check_screen.dart / admin_vacation_limits_setting_screen.dart / admin_approval_template_screen.dart 순. 마지막으로 widgets/admin_vacation_add_dialog.dart, widgets/today_schedule_dialog.dart, widgets/update_dialog.dart 등 다이얼로그류의 shadcn 버튼 교체.)

**진행 중 제외 파일**: login_screen.dart, register_screen.dart, widgets/vacation_request_dialog.dart는 다른 작업자가 현재 편집 중이므로 이번 감사에서는 스냅샷 상태만 기록하고 lib/ 코드는 일절 수정하지 않았다.
